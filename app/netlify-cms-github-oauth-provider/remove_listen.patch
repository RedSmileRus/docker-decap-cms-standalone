diff --git a/app.js b/app.js
--- a/app.js
+++ b/app.js
@@ -end_of_file (adjust line numbers as needed)
- app.listen(process.env.PORT || 3000, function () {
-   console.log('OAuth provider listening on port ' + (process.env.PORT || 3000));
- });
+ module.exports = app;  // Если ещё не добавлено